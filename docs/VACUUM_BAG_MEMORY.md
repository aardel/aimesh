# Vacuum Bag Memory

Vacuum Bag Memory keeps the active mind small while preserving useful knowledge.

Planned states:

- `active`: used often, loaded immediately
- `warm`: used sometimes, indexed
- `compressed`: summarized and source-linked
- `archived`: preserved but not searched by default
- `forgotten`: intentionally removed

The first prototype only includes a `VacuumBag` dataclass stub.
